services:
  - type: web
    name: phoneverse
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node automation-server.js
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 3000